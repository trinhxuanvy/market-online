export enum Gender {
  Male = 0,
  Female = 1,
}

export enum UserType {
  Admin = 0,
  Client = 1,
}

export enum Region {
  Green = 1,
  Yellow = 2,
  Orange = 3,
  Red = 4,
}

export enum StoreType {
  Food = 1,
  Clothes = 2,
  Electric = 3,
}

export enum PaymentType {
  Money = 1,
  Momo = 2,
  ZaloPay = 3,
}

export enum SaleOrderType {
  Approved = 1,
  Unapproved = 2,
}
