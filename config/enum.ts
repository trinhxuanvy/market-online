export enum Gender {
  Male = 0,
  Female = 1,
}

export enum UserType {
  Admin = 0,
  Client = 1,
}

export enum Region {
  Green = 1,
  Yellow = 2,
  Orange = 3,
  Red = 4,
}

export enum StoreType {
  Food = 1,
  Clothes = 2,
  Electric = 3,
}
